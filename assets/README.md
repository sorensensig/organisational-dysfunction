# assets

Store front matter for the root README.

## `banner.png` (drop it here)

The root `README.md` references `assets/banner.png` — the neon cyberpunk night
storefront banner (an AI adaptation of [Jesse Zhou's ramen shop](https://jesse-zhou.medium.com/jesses-ramen-case-study-77bae77ab5f0)).

Place the image at exactly `assets/banner.png`. It renders full-width, so a wide
aspect ratio (roughly 3:1 or 4:1) reads best. Until the file is added, the banner
`<img>` in the README simply shows its alt text.
